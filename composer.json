{
    "name": "groupe-speed-cloud/signatures",
    "description": "Générateur de signatures email avec authentification Google",
    "type": "project",
    "require": {
        "php": ">=8.0",
        "google/apiclient": "^2.15"
    },
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "scripts": {
        "post-install-cmd": [
            "mkdir -p uploads/signatures"
        ]
    },
    "config": {
        "sort-packages": true
    }
}
